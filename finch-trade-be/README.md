Finch-trade backend

Quickstart:

1. cp .env.example .env && fill values
2. npm install
3. npm run dev

Notes:
- Server entry: src/server.js
- DB file (if present) lives in src/db/ and is ignored by git
- Add tests in the src/tests folder
